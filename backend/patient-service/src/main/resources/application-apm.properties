# Elastic APM configuration for patient-service
elastic.apm.service_name=patient-service
elastic.apm.server_urls=http://apm-server:8200
elastic.apm.application_packages=com.hospital

# Optional: secret token if APM server requires it
# elastic.apm.secret_token=${ELASTIC_APM_SECRET_TOKEN}
